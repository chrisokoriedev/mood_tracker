name: mood_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod:
  riverpod_annotation:

  hive:
  hive_flutter:

  intl:

  flutter_screenutil:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator:
  build_runner:
  hive_generator: 
  riverpod_lint: 
  custom_lint: 


flutter:
  uses-material-design: true
